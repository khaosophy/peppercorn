import Button from './Button';

/* todo: get this working, including the many state variants */

const meta = {
  title: 'Button',
  component: Button,
};

export default meta;

export const Default = () => (
  <Button>
    Click Me
  </Button>
);