const routes = {
  home: '/',
  
  // auth
  login: '/login',
  register: '/register',
  signout: '/signout',
  
  // recipes
  recipes: '/recipes',
  newRecipe: '/recipes/new',
}

export default routes;